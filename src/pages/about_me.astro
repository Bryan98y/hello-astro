<!doctype html>
<html lang="es">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
        <link rel="icon" href="/favicon.ico" />
        <meta name="generator" content={Astro.generator} />
        <title>Bryan Yandún | Portafolio</title>

        <style>
            /* --- ESTILOS GLOBALES Y RESET --- */
            html,
            body {
                background-color: #fffff; /* Ajustado al color oscuro de tu diseño */
                margin: 0;
                padding: 0;
                width: 100%;
                overflow-x: hidden;
                font-family:
                    ui-sans-serif,
                    system-ui,
                    -apple-system,
                    sans-serif;
            }

            /* --- HEADER Y NAVEGACIÓN --- */
            .container {
                background-color: #091a28;
                padding: 0.8rem 2rem;
                width: 100%;
            }

            .navegacion {
                display: flex;
                align-items: center;
                justify-content: space-between;
                max-width: 1400px;
                margin: 0 auto;
            }

            .perfil {
                display: flex;
                align-items: center;
                gap: 12px;
                flex-shrink: 0;
            }

            .bryan {
                width: 45px;
                height: 45px;
                border-radius: 50%;
                border: 2px solid #fff;
                object-fit: cover;
            }

            .nombre {
                color: white;
                font-size: 1.2rem;
                margin: 0;
                white-space: nowrap;
            }

            .nav-principal {
                display: flex;
                gap: 2rem;
            }

            .nav-principal a {
                color: white;
                text-decoration: none;
                font-size: 1rem;
                font-weight: 500;
                position: relative;
                padding: 4px 0;
            }

            .nav-principal a::after {
                content: "";
                position: absolute;
                width: 0;
                height: 2px;
                bottom: 0;
                left: 0;
                background-color: #fff;
                transition: width 0.3s ease;
            }

            .nav-principal a:hover::after {
                width: 100%;
            }

            .redes nav {
                display: flex;
                gap: 1.5rem;
            }

            .redes img {
                width: 22px;
                height: 22px;
                cursor: pointer;
                filter: brightness(0) invert(1);
                transition:
                    transform 0.2s,
                    opacity 0.2s;
            }

            .redes img:hover {
                opacity: 0.8;
                transform: scale(1.1);
            }

            .red nav {
                display: inline-flex;
                gap: 1.5rem;
            }

            .red img {
                width: 22px;
                height: 22px;
                cursor: pointer;
                filter: brightness(0) invert(1);
                transition:
                    transform 0.2s,
                    opacity 0.2s;
            }

            .red img:hover {
                opacity: 0.8;
                transform: scale(1.1);
            }
            /* --- SECCIÓN HERO --- */
            .hero-container {
                background-color: #091a28;
                display: flex;
                justify-content: center;
                align-items: center;
                margin: 2rem auto;
                padding: 6rem 2rem;
                max-width: 1200px;
                border-radius: 12px;
                text-align: center;
            }

            .hero-content h1 {
                font-size: 3.5rem;
                color: white;
                margin: 0;
                line-height: 1.1;
            }

            .hero-content p {
                color: #94a3b8;
                font-size: 1.25rem;
                margin: 1.5rem 0;
            }

            /* --- RESPONSIVE --- */
            @media (max-width: 768px) {
                .navegacion {
                    flex-direction: column;
                    gap: 1.5rem;
                    padding: 1rem 0;
                }
                .nav-principal {
                    flex-wrap: wrap;
                    justify-content: center;
                    gap: 1rem;
                }
                .hero-content h1 {
                    font-size: 2.5rem;
                }
                .cta-content h2 {
                    font-size: 2.2rem;
                }
            }
        </style>
    </head>

    <body>
        <header>
            <div class="container">
                <div class="navegacion">
                    <div class="perfil">
                        <img src="/bryan.jpeg" alt="Bryan" class="bryan" />
                        <h2 class="nombre">Bryan Yandún</h2>
                    </div>

                    <nav class="nav-principal">
                        <a href="/">Inicio</a>
                        <a href="#sobre-mi">Sobre mí</a>
                        <a href="/">Contacto</a>
                        <a href="#proyectos">Proyectos</a>
                    </nav>

                    <div class="redes">
                        <nav>
                            <img src="/github.png" alt="GitHub" />
                            <img src="/linkedin.png" alt="LinkedIn" />
                            <img src="/instagram.png" alt="Instagram" />
                            <img src="/whatsapp.png" alt="WhatsApp" />
                        </nav>
                    </div>
                </div>
            </div>
        </header>

        <main>
            <section class="hero-container">
                <div class="hero-content">
                    <h1>Hola con todos!!</h1>
                    <p>Mi nombre es Bryan Yandún</p>
                </div>
            </section>
        </main>

        <footer>
            <div
                class="container"
                style="text-align: center; padding: 2rem; color: #94a3b8; font-size: 0.9rem;"
            >
                <p>&copy; 2026, Bryan Yandún, Quito, Ecuador</p>
                <div class="red">
                    <nav>
                        <img src="/github.png" alt="GitHub" />
                        <img src="/linkedin.png" alt="LinkedIn" />
                        <img src="/instagram.png" alt="Instagram" />
                        <img src="/whatsapp.png" alt="WhatsApp" />
                    </nav>
                </div>
            </div>
        </footer>
    </body>
</html>
