<!doctype html>
<html lang="es">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<link rel="icon" href="/favicon.ico" />
		<meta name="generator" content={Astro.generator} />
		<title>Astro Basics</title>
	</head>
	<body>
		<div class="stars"></div>
		<header>
			<div class="container">
				<div class="navegacion">
					<div class="perfil">
						<img src="/bryan.jpeg" alt="Bryan" class="bryan" />
						<h2 class="nombre">Bryan Yandún</h2>
					</div>

					<nav class="nav-principal">
						<a href="#inicio">Inicio</a>
						<a href="#sobre-mi">Sobre mí</a>
						<a href="#contacto">Contacto</a>
						<a href="#proyectos">Proyectos</a>
					</nav>

					<div class="redes">
						<nav>
							<img src="/github.png" alt="GitHub" />
							<img src="/linkedin.png" alt="LinkedIn" />
							<img src="/instagram.png" alt="Instagram" />
							<img src="/whatsapp.png" alt="WhatsApp" />
						</nav>
					</div>
				</div>
			</div>
		</header>
		<main class="hero">
    <div class="hero-content">
        <h1>Diseñando experiencias <span class="highlight">digitales únicas</span></h1>
        <p>Desarrollador enfocado en crear interfaces modernas, rápidas y accesibles.</p>
        <div class="hero-buttons">
            <a href="#proyectos" class="btn-primary">Ver mis proyectos</a>
            <a href="#contacto" class="btn-secondary">Hablemos</a>
        </div>
    </div>
</main>

<style>
    .hero {
        height: 80vh; /* Ocupa casi toda la pantalla inicial */
        display: flex;
        align-items: center;
        justify-content: center;
        text-align: center;
        padding: 0 2rem;
    }

    .hero-content h1 {
        font-size: 3.5rem;
        color: white;
        max-width: 800px;
        line-height: 1.1;
    }

    .highlight {
        color: #3498db; /* Un azul llamativo para resaltar */
    }

    .hero-content p {
        color: #94a3b8;
        font-size: 1.25rem;
        margin: 1.5rem 0 2rem;
    }

    .hero-buttons {
        display: flex;
        gap: 1rem;
        justify-content: center;
    }

    .btn-primary {
        background-color: white;
        color: #091a28;
        padding: 0.8rem 1.5rem;
        border-radius: 8px;
        text-decoration: none;
        font-weight: bold;
    }

    .btn-secondary {
        border: 1px solid white;
        color: white;
        padding: 0.8rem 1.5rem;
        border-radius: 8px;
        text-decoration: none;
    }
</style>
	</body>
</html>

<style>
	/* Media Query para móviles */
@media (max-width: 768px) {
    .navegacion {
        flex-direction: column; /* Cambia de fila a columna */
        gap: 1.5rem;            /* Espacio entre los bloques amontonados */
        padding: 1rem 0;
    }

    .nav-principal {
        flex-wrap: wrap;       /* Si los links son largos, saltan de línea */
        justify-content: center;
        gap: 1rem;
    }

    .nav-principal a {
        font-size: 0.95rem;    /* Un poco más pequeño para móviles */
    }

    .redes {
        justify-content: center;
        width: 100%;
    }
}
	html,
	body {
		background-color: #1f2937;
		margin: 0;
		padding: 0;
		width: 100%;
		overflow-x: hidden; /* Evita scroll lateral accidental */
	}
	/* Reset básico para evitar espacios extra */

	.container {
		background-color: #091a28;
		padding: 0.8rem 2rem;
		width: 100%;
	}

	.navegacion {
		display: flex;
		align-items: center;
		justify-content: space-between; /* Esto crea el espacio entre los 3 bloques */
		max-width: 1400px;
		margin: 0 auto;
		font-family:
			ui-sans-serif,
			system-ui,
			-apple-system,
			BlinkMacSystemFont,
			"Segoe UI",
			Roboto,
			"Helvetica Neue",
			Arial,
			"Noto Sans",
			sans-serif;
	}

	/* IZQUIERDA: Foto y Nombre alineados */
	.perfil {
		display: flex;
		align-items: center;
		gap: 12px;
		flex-shrink: 0; /* Evita que el nombre se achique */
	}

	.bryan {
		width: 45px;
		height: 45px;
		border-radius: 50%;
		border: 2px solid #fff;
		object-fit: cover;
	}

	.nombre {
		color: white;
		font-size: 1.2rem;
		margin: 0;
		white-space: nowrap; /* Evita que el nombre se rompa en dos líneas */
	}

	/* CENTRO: Menú de navegación */
	.nav-principal {
		display: flex;
		gap: 2rem; /* Espacio entre palabras */
	}

	.nav-principal a {
		font-family:
			ui-sans-serif,
			system-ui,
			-apple-system,
			BlinkMacSystemFont,
			"Segoe UI",
			Roboto,
			"Helvetica Neue",
			Arial,
			"Noto Sans",
			sans-serif;

		color: white;
		text-decoration: none;
		font-size: 1rem;
		font-weight: 500;
		position: relative;
		padding: 4px 0;
	}

	/* El efecto de línea que querías */
	.nav-principal a::after {
		content: "";
		position: absolute;
		width: 0;
		height: 2px;
		bottom: 0;
		left: 0;
		background-color: #fff;
		transition: width 0.3s ease;
	}

	.nav-principal a:hover::after {
		width: 100%;
	}

	/* DERECHA: Iconos de Redes */
.redes nav {
    display: flex;
    gap: 1.5rem; /* Aumenté un poco el espacio para que se vea mejor */
    align-items: center;
}

.redes img {
    width: 22px;
    height: 22px;
    cursor: pointer;
    filter: brightness(0) invert(1);
    transition: transform 0.2s, opacity 0.2s;
}

.redes img:hover {
    opacity: 0.8;
    transform: scale(1.1); /* Un pequeño efecto de aumento al pasar el cursor */
}
</style>
